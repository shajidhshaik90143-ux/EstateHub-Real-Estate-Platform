document.addEventListener('DOMContentLoaded',()=>{const u=getStore('user',null);if(!u){toast('Please sign in first');setTimeout(()=>location.href='login.html',500);return}$('#welcomeUser').textContent=`Welcome, ${u.name}`;$('#favCount').textContent=getStore('favorites').length;$('#compareCount').textContent=getStore('compare').length;$('#recentCount').textContent=getStore('recent').length;$('#logoutBtn').onclick=()=>{localStorage.removeItem('user');location.href='index.html'}})
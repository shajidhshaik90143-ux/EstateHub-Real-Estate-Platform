function userName(){return getStore('user',null)?.name||'Guest'}
document.addEventListener('DOMContentLoaded',()=>{const lf=document.querySelector('#loginForm');if(lf)lf.onsubmit=e=>{e.preventDefault();const email=$('#loginEmail').value;const user=getStore('user',{});setStore('user',{name:user.name||email.split('@')[0],email});location.href='dashboard.html'};const sf=document.querySelector('#signupForm');if(sf)sf.onsubmit=e=>{e.preventDefault();setStore('user',{name:$('#signupName').value,email:$('#signupEmail').value});toast('Account created');setTimeout(()=>location.href='dashboard.html',600)}})